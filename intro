#!/bin/bash
#retrieve name and arguments
self[0]=`basename "$0"`;
self[1]=$1;
self[2]=$2;
self[3]=$3;
self[4]=$4;
self[5]=$5;
for i in ${self[@]}; do echo $i; slength=$slength+1; done
#**printf '%s\n' "${self[@]}";
#**$slength = 0#'**$COUNTER = 0
#**while [ $COUNTER -lt 9 ]; do 
#**{
#**echo the counter is $COUNTER;
#**{
#**if [ $self[$COUNTER]!="" ]; then { $slength=$COUNTER; let COUNTER+=1; }
#**fi
#**}
#**} done;
printf 'slength:%s\n' "$slength";
